cd backend
flask run --host=0.0.0.0
cd frontend
npx expo start
npx expo start -c

#starting the website
change the ip address in the appi.js
open terminal
cd backend
flask run

then open the login.html (by clicking on LiveServer after opening the file)

python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
set FLASK_APP=app.py
 flask run --host=0.0.0.0 --port=5000